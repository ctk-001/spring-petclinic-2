pipeline {
    agent any
  
  stages {
    stage('Git fatch') {
      
      steps {
        echo 'Git fatch'
        git url: 'https://github.com/SooYong-Jeong/spring-petclinic.git', 
          branch: 'main', credentialsId: 'gitCredential'
      }
    }
      stage('Maven build') {
      
      steps {
        echo 'Maven build'
      }
    }
      stage('Docker image') {
      
      steps {
        echo 'Docker image'
      }
    }
      stage('Image push') {
      
      steps {
        echo 'Image push'
      }
    }
      stage('Codedeploy') {
      
      steps {
        echo 'Codedeploy'
      }
    }
  }
}
